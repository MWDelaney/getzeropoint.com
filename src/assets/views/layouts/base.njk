<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    {# Page Title #}
    <title>{{ title | safe }}</title>

    {# Stylesheet #}
    <link rel="stylesheet" href="{{ '/assets/styles/styles.css' | url }}"/>

    {# Favicon #}
    <link rel="icon" type="image/svg+xml" href="/assets/images/getzeropoint.com-icon.svg">

    {# metagen plugin output #}
    {% metagen
        title=title or metadata.title,
        desc=desc or metadata.desc,
        url=url + page.url,
        img=image,
        img_alt=alt,
        twitter_card_type=type,
        twitter_handle=twitter,
        name=name
    %}

    {# If ELEVENTY_ENV environmental variable isn't set to "production", hide this page from search engines #}
    {% if ( index == false) %}
      <meta name="robots" content="noindex,nofollow">
    {% endif %}

  </head>
  <body>
    {# Skip to content link #}
    <a class="visually-hidden" href="#main">Skip to content</a>

    {# Site header with navigation #}
    {# <header>
      {% include "partials/navigation.njk" %}
    </header> #}

    {# Main content #}
    <main id="main" tabindex="-1">
      {{ content | safe }}
    </main>

    {# Site footer with copyright #}
    <footer id="footer">
      {% year %} &copy; {{ site.siteName }}
    </footer>

    {# Script bundle #}
    <script src="{{ '/assets/scripts/main.js' | url }}"></script>
  </body>
</html>
